{
  "name": "bianca-house-color-simulator",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "node server.js --dev",
    "start": "node server.js",
    "build": "vite build",
    "test": "playwright test tests/smoke.spec.js --project=chrome --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "prettier": "^3.9.4",
    "vite": "^7.0.2"
  }
}
